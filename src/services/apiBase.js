// Use Netlify env in prod, fall back to /api for local dev proxy
export const API_BASE = "https://happy-thoughts-api-4ful.onrender.com";
export const USE_MOCK = false; // toggle if you need offline dev
